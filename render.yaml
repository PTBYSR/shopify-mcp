services:
  - type: web
    name: shopify-mcp
    env: node
    plan: starter
    buildCommand: npm install && npm run build && npm run build:http
    startCommand: node dist/server-http.js
    envVars:
      - key: SHOPIFY_ACCESS_TOKEN
        sync: false
      - key: MYSHOPIFY_DOMAIN
        sync: false
      - key: PORT
        value: 3000

